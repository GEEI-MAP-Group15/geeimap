
<div class="row" align="center">

	{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary btn-lg col-lg-12 align-top">{{ button_label|default('Save changes') }}</button>
	{{ form_end(form) }}

</div>
