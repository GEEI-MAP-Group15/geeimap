{% extends 'base.html.twig' %}

{% block title %}AcademicLevel{% endblock %}

{% block body %}
    <h1>AcademicLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academic_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academic_level.name }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ academic_level.capacity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('academic_level_index') }}">back to list</a>

    <a href="{{ path('academic_level_edit', {'id': academic_level.id}) }}">edit</a>

    {{ include('admission/academic_level/_delete_form.html.twig') }}
{% endblock %}
