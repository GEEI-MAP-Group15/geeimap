{% extends "base.html.twig" %}

{% block title %}Application{% endblock %}

{% block body %}
	<div class="card mb-3">
       <div class="card-body bg-light">
           <div class="row">
               <div class="col">
                   <div class="row">
                       <div class="col-6 text-center">
                        <a href="{{ path('applicant_page_show', {'id': application.id}) }}">
                           {{ application.staffuser.username }}</a>
                           <br>
                           <small>username</small>
                       </div>
                       <div class="col-6 text-center">
                           {{ application.status }}
                           <br>
                           <small>Status</small>
                       </div>
                       
                   </div>
               </div>
           </div>
       </div>
   </div>
{% endblock %}